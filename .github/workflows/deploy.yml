name: Deploy portfolio a GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar, construir y preparar Astro
        uses: withastro/action@v3
        with:
          node-version: 22
          package-manager: npm@latest
        env:
          # .env está en .gitignore (no se publica), así que hay que pasar la variable
          # aquí explícitamente. Configúrala en Settings → Secrets and variables →
          # Actions → Variables → New repository variable. No es información sensible
          # (el ID de medición es público en el HTML de cualquier página), por eso es
          # una "variable" y no un "secret".
          PUBLIC_GA_MEASUREMENT_ID: ${{ vars.PUBLIC_GA_MEASUREMENT_ID }}

      - name: Empaquetar artefacto de Pages
        run: |
          tar \
            --dereference --hard-dereference \
            --directory ./dist \
            -cvf "$RUNNER_TEMP/artifact.tar" \
            --exclude=.git \
            --exclude=.github \
            .

      - name: Subir artefacto de Pages
        uses: actions/upload-artifact@v4
        with:
          name: github-pages
          path: ${{ runner.temp }}/artifact.tar
          retention-days: 1
          overwrite: true

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Publicar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4