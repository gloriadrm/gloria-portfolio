---
interface Props {
  label: string;
}
const { label } = Astro.props;
---
<span class="badge badge-sm h-auto min-h-[1.625rem] font-label bg-base-200 border-base-300 text-primary px-3 py-2 whitespace-normal text-left leading-snug">{label}</span>
